import React, { Component } from 'react';
import { View, StyleSheet, Text} from 'react-native';
import styles from './styles';
import * as strings from './strings.js';
//import * as commonStyles from './../res/defaultstyles';
//import * as commonString from './../res/strings';


/**
 * Root Screen of the module.
 */
class class <%= props.name %> extends Component {

	constructor(props) {
		super(props);
		// Define your local component states here
		this.state = { };
	};

	/**
	 * Render views on every state change.
	 */
	render() {
		return (
			<View>
				<Text> "Module coming soon" </Text> 
			</View>
		)
	}
}

export default RootComponent;
