import * as types from './types';

/**
 * Define your actions as functions and c
 * code should be in below mentioned format
 */
const authenticateUser = (username, password) => {
    return {
        type: types.AUTHENTICATING_USER,
    }
}

export {authenticateUser};;
